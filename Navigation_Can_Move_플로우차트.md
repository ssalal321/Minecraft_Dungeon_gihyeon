# Navigation::Can_Move() 플로우차트

## 📋 함수 개요

**함수명**: `CNavigation::Can_Move(_fvector vWorldPos)`  
**목적**: 주어진 월드 좌표 위치로 이동 가능한지 판단  
**반환값**: `true` (이동 가능) / `false` (이동 불가능)

---

## 🔄 메인 플로우 (Can_Move)

```
┌─────────────────────────────────────────────┐
│ 시작: Can_Move(vWorldPos)                  │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ [1단계] 좌표 변환                            │
│ - 월드 좌표 → 로컬 좌표 변환                 │
│ - vLocalPos 계산                             │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ [2단계] 초기화                                │
│ - iCellIndex = m_iCurrentCellIndex          │
│ - iNextNeighbor = -1                         │
│ - depth = 0                                  │
└─────────────────────────────────────────────┘
                    ↓
        ┌───────────────────────┐
        │ [3단계] 반복문 시작     │
        │ for (depth = 0 ~ 9)    │
        └───────────────────────┘
                    ↓
        ┌───────────────────────┐
        │ depth < 10?            │
        └───────────────────────┘
            YES ↓        NO ↓
    ┌────────────┐   ┌──────────────┐
    │현재 셀 가져오기│   │ return false │
    │pCurrent =   │   │ (이동 불가)   │
    │m_Cells[i]   │   └──────────────┘
    └────────────┘
            ↓
    ┌───────────────────────┐
    │ [4단계] 셀 내부 확인    │
    │ Is_In(vLocalPos) 호출  │
    └───────────────────────┘
            ↓
    ┌───────────────────────┐
    │ 위치가 셀 내부인가?     │
    └───────────────────────┘
        YES ↓        NO ↓
┌───────────────┐  ┌──────────────┐
│ [5단계]       │  │인접 셀 존재? │
│ 셀 내부 처리   │  └──────────────┘
└───────────────┘      YES ↓    NO ↓
        ↓          ┌────────┐ ┌───────┐
┌───────────────┐  │다음 셀로│ │return │
│ Is_Locked()?  │  │이동    │ │false  │
└───────────────┘  └────────┘ └───────┘
    YES ↓    NO ↓       ↓
┌───────┐ ┌─────────────┐   │
│return │ │[6단계]       │   │
│false  │ │Decided_Next │   │
└───────┘ │_Index() 호출│   │
          └─────────────┘   │
              ↓              │
    ┌─────────────────┐      │
    │ 성공?           │      │
    └─────────────────┘      │
      YES ↓      NO ↓        │
┌──────────┐ ┌──────────┐   │
│인덱스     │ │인접 셀   │   │
│업데이트   │ │존재?     │   │
│return    │ └──────────┘   │
│true      │   YES ↓        │
└──────────┘ ┌───────┐      │
             │다음 셀│      │
             └───────┘      │
                 ↓          │
         ┌──────────────┐   │
         │ depth++      │◄──┘
         └──────────────┘
```

---

## 📝 단계별 상세 설명

### 1단계: 좌표 변환
```cpp
_matrix WorldMatrixInv = XMMatrixInverse(nullptr, XMLoadFloat4x4(m_pWorldMatrix));
_vector vLocalPos = XMVector3TransformCoord(vWorldPos, WorldMatrixInv);
```
- 월드 좌표를 로컬 좌표로 변환
- Navigation Mesh의 Transform에 맞춰 변환

### 2단계: 초기화
```cpp
_int iCellIndex = m_iCurrentCellIndex;
_int iNextNeighbor = -1;
```
- 현재 셀 인덱스부터 탐색 시작
- 인접 셀 정보 초기화

### 3단계: 반복 탐색 (최대 10회)
```cpp
for (_int depth = 0; depth < 10; ++depth)
{
    CCell* pCurrent = m_Cells[iCellIndex];
    // ...
}
```
- 깊이 제한: 무한 루프 방지
- 현재 셀부터 시작하여 인접 셀로 확장 탐색

### 4단계: 셀 내부 확인
```cpp
if (pCurrent->Is_In(vLocalPos, &iNextNeighbor, nullptr))
{
    // 셀 내부
}
else
{
    // 셀 외부
}
```

### 5단계: 셀 내부 처리
```
┌─────────────────────┐
│ Is_Locked() 체크    │
└─────────────────────┘
    잠금? ↓    ↓ 아니면
┌──────────┐ ┌──────────────┐
│return    │ │Decided_Next  │
│false     │ │_Index() 호출 │
└──────────┘ └──────────────┘
```

### 6단계: Decided_Next_Index() 호출
- 현재 셀과 인접 셀 중 이동 가능한 셀 선택
- 성공 시 `m_iCurrentCellIndex` 업데이트 후 `true` 반환

---

## 🔍 Decided_Next_Index() 플로우

```
┌────────────────────────────────┐
│ Decided_Next_Index()           │
│ iCurIndex, iNeighborIndex      │
└────────────────────────────────┘
            ↓
┌─────────────────────┐
│ nullptr 체크        │
└─────────────────────┘
            ↓
┌─────────────────────┐
│ 현재 셀 확인         │
│ Classify_Cell()     │
└─────────────────────┘
            ↓
    ┌───────────────┐
    │ WALKABLE?     │
    └───────────────┘
      YES ↓      NO ↓
┌──────────┐ ┌──────────────┐
│현재 셀    │ │인접 셀 확인   │
│사용      │ │Classify_Cell │
│return    │ └──────────────┘
│true      │        ↓
└──────────┘ ┌──────────────┐
             │ WALKABLE?    │
             └──────────────┘
               YES ↓    NO ↓
         ┌────────┐ ┌───────┐
         │인접 셀 │ │return │
         │사용    │ │false  │
         │return  │ └───────┘
         │true    │
         └────────┘
```

---

## 🏷️ Classify_Cell() 플로우

```
┌────────────────────────┐
│ Classify_Cell(pCell)   │
└────────────────────────┘
        ↓
┌───────────────┐
│ nullptr 체크  │
└───────────────┘
        ↓
┌───────────────┐
│ 평면 노말 가져오기│
│ Get_PlaneNormal│
└───────────────┘
        ↓
┌───────────────┐
│ Is_Wall()     │
│ normal.y      │
│ < 0.5f?       │
└───────────────┘
  YES ↓    NO ↓
┌──────┐ ┌──────────┐
│WALL  │ │WALKABLE  │
└──────┘ └──────────┘
```

**판단 기준**:
- `normal.y >= 0.5f` → `WALKABLE` (이동 가능, 경사가 완만함)
- `normal.y < 0.5f` → `WALL` (이동 불가, 벽/가파른 경사)

---

## 📊 전체 처리 흐름 요약

```
Can_Move(vWorldPos)
    │
    ├─▶ 좌표 변환 (월드 → 로컬)
    │
    ├─▶ 반복 탐색 (depth 0~9)
    │   │
    │   ├─▶ 현재 셀에서 Is_In() 호출
    │   │
    │   ├─▶ [셀 내부]
    │   │   ├─▶ Is_Locked() → false (잠김)
    │   │   └─▶ Decided_Next_Index()
    │   │       ├─▶ Classify_Cell() → WALKABLE?
    │   │       └─▶ true → 인덱스 업데이트, return true
    │   │
    │   └─▶ [셀 외부]
    │       └─▶ 인접 셀로 이동 (iNextNeighbor)
    │
    └─▶ return false (이동 불가)
```

---

## ✅ 반환값 의미

### `true` 반환
- ✅ 이동 가능한 위치
- ✅ `m_iCurrentCellIndex` 업데이트됨
- ✅ 다음 프레임에서 업데이트된 인덱스 사용

### `false` 반환
- ❌ 셀이 잠겨있음 (`Is_Locked()`)
- ❌ 이동 가능한 셀을 찾지 못함
- ❌ 벽/가파른 경사면 (`WALL` 타입)
- ❌ 탐색 범위 초과 (10 depth 이상)

---

## 🔑 핵심 포인트

1. **좌표 변환**: 월드 → 로컬 (Navigation Mesh Transform 적용)
2. **깊이 제한**: 최대 10 depth 탐색 (성능 최적화)
3. **잠금 체크**: `Is_Locked()`로 셀 잠금 상태 확인
4. **타입 분류**: `Classify_Cell()` → WALKABLE/WALL 구분
5. **경사도 판단**: `normal.y < 0.5f` → 벽으로 처리

---

## 💡 예시 시나리오

### 시나리오 1: 정상 이동
```
현재 셀 내부 → Is_Locked() = false 
→ Decided_Next_Index() 성공 
→ return true ✅
```

### 시나리오 2: 셀 경계 이동
```
현재 셀 외부 → 인접 셀로 이동 
→ 인접 셀 내부 확인 
→ return true ✅
```

### 시나리오 3: 잠긴 셀
```
현재 셀 내부 → Is_Locked() = true 
→ return false ❌
```

### 시나리오 4: 벽 충돌
```
현재 셀 내부 → Classify_Cell() = WALL 
→ return false ❌
```