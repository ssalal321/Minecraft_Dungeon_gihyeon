# Minecraft Dungeons 프로젝트 컴포넌트 구조 기술서

## 1. 프로젝트 개요

본 프로젝트는 C++와 DirectX 11 기반의 컴포넌트 시스템을 활용한 3D 액션 게임 엔진으로 구성되어 있습니다. 
컴포넌트 기반 설계와 FSM(Finite State Machine)을 통해 확장 가능하고 유지보수가 용이한 게임 구조를 구현하였습니다.

## 2. 전체 아키텍처

### 2.1 계층 구조
```
┌─────────────────────────────────────────┐
│          Client Layer (게임 로직)          │
│  - Player, Monster, Level, State 등     │
└─────────────────────────────────────────┘
                    ↕
┌─────────────────────────────────────────┐
│        Engine Layer (엔진 코어)          │
│  - Component, GameObject, Manager 등    │
└─────────────────────────────────────────┘
                    ↕
┌─────────────────────────────────────────┐
│      DirectX 11 / Third-party (API)     │
└─────────────────────────────────────────┘
```

## 3. 컴포넌트 기반 아키텍처

### 3.1 컴포넌트 시스템의 핵심 이점

#### **왜 컴포넌트 기반 구조인가?**

본 프로젝트는 **컴포넌트 기반 설계(Component-Based Design)**를 채택하여 다음과 같은 핵심 장점을 제공합니다:

**1. 기능의 모듈화 및 재사용성**
- 각 기능(Transform, Collider, Navigation, Animation 등)이 독립적인 컴포넌트로 구현
- 특정 기능만 필요한 오브젝트에 선택적으로 부착 가능
- 동일한 컴포넌트를 다양한 게임오브젝트에 재사용

**2. 느슨한 결합(Loose Coupling)**
- 컴포넌트 간 직접적인 의존성 제거
- 게임오브젝트는 컴포넌트의 내부 구현을 알 필요 없음
- 인터페이스를 통한 통신으로 유지보수성 향상

**3. 런타임 유연성**
- 실행 시점에 컴포넌트 동적 추가/제거
- 오브젝트의 행동을 런타임에 변경 가능
- 메모리 효율적인 기능 관리

**4. 확장성 및 테스트 용이성**
- 새로운 기능 추가 시 기존 코드 수정 최소화
- 컴포넌트 단위로 독립적인 테스트 가능
- 디버깅 및 병렬 개발 용이

### 3.2 실제 구현 사례

#### **CPlayer의 컴포넌트 구성**

```cpp
// Player.cpp - Ready_Components() 함수
HRESULT CPlayer::Ready_Components()
{
    // 1. Navigation 컴포넌트 - 레벨별로 선택적 추가
    if (LevelIndex == LEVEL_LOUNGE)
    {
        Add_Component(LEVEL_STATIC, 
            TEXT("Prototype_Component_Navigation_LoungeMap"),
            TEXT("Com_Navigation_LoungeMap"),
            reinterpret_cast<CComponent**>(&m_pNavigationCom));
    }
    
    // 2. Transform은 모든 게임오브젝트에 자동 추가
    // 3. Model, Collider 등도 필요에 따라 추가
}
```

**분석:**
- `CNavigation` 컴포넌트는 특정 레벨에만 필요하므로 선택적으로 추가
- 플레이어가 어떤 레벨에 있든, 필요한 기능만 부착하여 메모리 절약
- 컴포넌트 추가 로직이 명확하고 유연함

#### **컴포넌트 관리 방식**

```cpp
// GameObject.h
class CGameObject
{
protected:
    map<const _wstring, class CComponent*> m_Components;  // 컴포넌트 저장소
    
public:
    CComponent* Find_Component(const _wstring& strComponentTag);  // 컴포넌트 검색
    CComponent* Add_Component(..., const _wstring& strComponentTag, ...);  // 컴포넌트 추가
    void Erase_Component(const wstring& strComponentTag);  // 컴포넌트 제거
};
```

**장점:**
1. **Tag 기반 관리**: 문자열 태그로 컴포넌트를 쉽게 찾고 관리
2. **중복 방지**: 동일 태그의 컴포넌트는 하나만 존재
3. **동적 확장**: 필요 시 런타임에 컴포넌트 추가/제거

### 3.3 주요 컴포넌트의 실제 활용

#### **1. CTransform - 공통 기능의 표준화**

```cpp
// 모든 GameObject는 자동으로 Transform 컴포넌트 보유
CGameObject::Initialize(void* pArg)
{
    // Transform은 항상 자동 추가
    m_pTransformCom = CTransform::Create(m_pDevice, m_pContext);
    m_Components.emplace(g_strTransformTag, m_pTransformCom);
}
```

**왜 좋은가?**
- 위치, 회전, 스케일 처리를 모든 오브젝트에서 동일한 방식으로 수행
- World Matrix 관리가 일관되고 예측 가능
- Transform 로직 수정 시 모든 오브젝트에 자동 반영

#### **2. CCollider - 충돌 감지의 독립성**

```cpp
// Collider 컴포넌트는 독립적으로 작동
_bool CCollider::Intersect(CCollider* pTargetCollider)
{
    // Collider는 Transform과 분리되어 작동
    // Collision Manager가 모든 Collider를 관리
    m_bIsCollision = m_pBounding->Intersect(...);
    return m_bIsCollision;
}

void CCollider::Update()
{
    // 자체적으로 World Matrix 업데이트
    _matrix mCombinedWorldMatrix = XMLoadFloat4x4(m_CombinedWorldMatrix);
    m_pBounding->Update(mCombinedWorldMatrix);
}
```

**실제 이점:**
- **충돌 로직 분리**: 충돌 감지는 Collision Manager가, 게임 로직은 GameObject가 담당
- **활성화/비활성화**: 필요 시 `Set_ColliderActive(false)`로 일시적으로 충돌 감지 비활성화
- **다중 충돌체 지원**: 하나의 GameObject가 여러 개의 Collider 보유 가능 (Ex: 몬스터의 Body Collider + Attack Range)

#### **3. CNavigation - 네비게이션의 선택적 사용**

```cpp
// Navigation은 필요할 때만 추가
if (m_pNavigationCom)
    m_pNavigationCom->SetUp_On_Navigation(m_pTransformCom);

// 특정 상황에서만 Navigation 사용
void CTransform::Go_Straight(_float fTimeDelta, CNavigation* pNavigation, _float fSpeedFactor)
{
    _vector vPosition = Get_State(STATE_POSITION);
    _vector vLook = Get_State(STATE_LOOK);
    
    if (pNavigation)
    {
        // Navigation이 있으면 제약된 이동
        vPosition += XMVector3Normalize(vLook) * m_fSpeedPerSec * fSpeedFactor * fTimeDelta;
        _vector vSlidingPos = {};
        
        if (!pNavigation->Can_Slide(vPrevPos, vPosition, vSlidingPos))
            return;
    }
    else
    {
        // Navigation이 없으면 자유 이동
        vPosition += XMVector3Normalize(vLook) * m_fSpeedPerSec * fSpeedFactor * fTimeDelta;
    }
}
```

**핵심 가치:**
- **가상의 땅**: Navigation Mesh를 이용하여 적절한 지형 위에서만 이동 가능
- **A* 경로 탐색**: Navigation Mesh의 Cell 네트워크를 활용한 경로 탐색
- **선택적 로딩**: Navigation 데이터가 없는 레벨에서는 자유로운 이동

#### **4. Animation & Model - 렌더링과 로직의 분리**

```cpp
// Model 컴포넌트는 렌더링 데이터만 관리
class CModel : public CComponent
{
    // 메시 데이터, 텍스처, 머티리얼
    // 애니메이션 시스템과 분리
};

// Animation 컴포넌트는 애니메이션만 관리
class CAnimation : public CBase
{
    // 키프레임, 채널, 블렌딩
    // Model과 협력하여 최종 렌더링
};
```

**장점:**
- **책임 분리**: Model은 데이터 관리, Animation은 시간에 따른 변환 관리
- **성능 최적화**: 애니메이션이 필요 없는 오브젝트는 Animation 컴포넌트 없이도 작동
- **재사용성**: 동일한 모델에 다양한 애니메이션 적용 가능

### 3.4 PartObject 시스템과의 통합

```cpp
// 복잡한 오브젝트는 PartObject로 분해
class CContainerObject : public CGameObject
{
    map<const _wstring, CPartObject*> m_PartObjects;  // 부품들
    
    // 예: 플레이어 = Body Part + Weapon Part + Armor Parts
    HRESULT Add_PartObject(..., const _wstring& strPartObjectTag, ...);
};

// PartObject도 독립적인 컴포넌트 보유
class CPartObject : public CGameObject
{
    // Parent World Matrix 참조
    const _float4x4* m_pParentWorldMatrix;
    
    // 자신만의 Transform, Model, Collider 등 보유
};
```

**실제 사용 예시:**

```cpp
// Player.cpp - PartObjects 준비
HRESULT CPlayer::Ready_PartObjects()
{
    // 1. Body Part 추가
    CBody_Player::BODY_PLAYER_DESC BodyDesc{};
    BodyDesc.pParentWorldMatrix = m_pTransformCom->Get_WorldMatrix_Ptr();
    BodyDesc.pState = &m_iState;
    Add_PartObject(..., TEXT("Part_Body"), &BodyDesc);
    
    // 2. Weapon Part 추가 (선택적)
    // Body의 소켓에 Weapon 부착
    CItem::ITEM_DESC WeaponDesc{};
    WeaponDesc.pSocketMatrix = pBody->Get_CombinedTransformationMatrix("J_R_Weapon");
    Add_PartObject(..., TEXT("Part_Weapon"), &WeaponDesc);
    
    // 각 PartObject는 독립적인 Collider, Model 보유
    // 부모의 World Matrix를 참조하여 정확한 위치 계산
}
```

**Why It's Good:**
- **계층적 구조**: ContainerObject > PartObject > Component (3단계)
- **분리된 충돌**: Body Collider와 Weapon Collider를 독립적으로 관리
- **소켓 시스템**: 본(Bone) 기반 소켓으로 무기 부착 위치 정밀 제어
- **동적 부품 교체**: 런타임에 무기/방어구 교체 가능

### 3.5 컴포넌트 시스템 활용의 핵심 가치

#### **실제 개발에서 발견한 이점들**

**1. Collider 컴포넌트의 분리로 인한 효율성**

```cpp
// Collision_Manager는 Collider 컴포넌트만 관리
void CCollision_Manager::Update()
{
    // 레벨별로 모든 Collider를 순회하며 충돌 검사
    for (auto& pCollider : m_Colliders[m_iCurrentLevelIndex])
    {
        if (!pCollider->Get_ColliderActive())
            continue;
            
        pCollider->Update();  // Bounding 업데이트
        
        for (auto& pOther : m_Colliders[m_iCurrentLevelIndex])
        {
            if (pCollider->Intersect(pOther))
                pCollider->Collided_With(pOther);
        }
    }
}
```

**효과:**
- GameObject는 Collision 처리 로직을 몰라도 됨
- Collider의 활성/비활성을 런타임에 제어 가능
- Collision Manager가 모든 충돌을 중앙 집중 관리
- 충돌 감지와 게임 로직의 완전한 분리

**2. Transform 컴포넌트의 자동 관리**

```cpp
// PartObject의 경우 Parent World Matrix 참조
void CPartObject::Update(_float fTimeDelta)
{
    // 부모의 World Matrix를 참조하여 자신의 위치 결정
    _float4x4 ParentWorldMatrix = *m_pParentWorldMatrix;
    _float4x4 CombinedMatrix = ParentWorldMatrix * m_pTransformCom->Get_WorldMatrix();
    
    // Weapon은 Body의 부착 위치에 따라 자동으로 이동
}
```

**효과:**
- 무기 교체 시 Transform만 변경하면 자동으로 올바른 위치에 배치
- Parent-Child 관계가 명확하게 정의됨
- 본(Bone) 기반 소켓 시스템과 완벽한 통합

**3. Navigation 컴포넌트의 선택적 사용**

```cpp
// 레벨별로 Navigation 컴포넌트가 다름
HRESULT CPlayer::Ready_Components()
{
    switch (LevelIndex)
    {
    case LEVEL_LOUNGE:
        Add_Component(..., TEXT("Prototype_Component_Navigation_LoungeMap"), ...);
        break;
        
    case LEVEL_SOGGYSWAMP:
        Add_Component(..., TEXT("Prototype_Component_Navigation_SoggySwamp"), ...);
        break;
        
    // Navigation이 없는 레벨도 있음
    }
}
```

**효과:**
- 레벨별로 적합한 Navigation Mesh 로딩
- Navigation 데이터가 없는 레벨에서도 정상 작동
- 메모리 효율성: 현재 레벨에 필요한 데이터만 로딩

### 3.6 컴포넌트 시스템이 해결한 주요 문제들

#### **문제 1: 다양한 오브젝트 타입의 공통 기능 중복**

**전통적 접근:**
```cpp
class CPlayer : public CGameObject
{
    _float3 m_Position;      // Transform 기능
    _float3 m_Rotation;     // Transform 기능
    _float3 m_Scale;        // Transform 기능
    CBounding* m_pBounding;  // Collision 기능
    CNavigation* m_pNavigation;  // Movement 기능
    // ... 엄청난 중복 코드
};

class CMonster : public CGameObject
{
    _float3 m_Position;      // 동일한 Transform 기능
    _float3 m_Rotation;     // 동일한 Transform 기능
    _float3 m_Scale;        // 동일한 Transform 기능
    CBounding* m_pBounding;  // 동일한 Collision 기능
    // ... 거의 동일한 코드
};
```

**컴포넌트 시스템:**
```cpp
class CGameObject
{
    map<wstring, CComponent*> m_Components;  // 필요한 것만 추가
    
    // Player는 Navigation도, Monster는 필요 없음
};

// Transform은 한 번만 구현, 모든 오브젝트에 자동 추가
```

**절감 효과:**
- 코드 중복 제거: Transform, Collider 로직을 한 번만 작성
- 유지보수 용이: Transform 로직 수정 시 모든 오브젝트에 자동 반영
- 메모리 최적화: 필요한 컴포넌트만 로딩

#### **문제 2: 오브젝트별 기능의 차이로 인한 복잡성**

**상속 기반 접근 (불가능):**
```cpp
// Player는 Navigation이 필요한데, 기타 오브젝트는 필요 없음
// → 상속으로는 해결 불가능
class CGameObject_WithNavigation : public CGameObject { ... };
class CGameObject_WithoutNavigation : public CGameObject { ... };
// → 수많은 조합으로 인한 클래스 폭발
```

**컴포넌트 시스템:**
```cpp
// 필요에 따라 컴포넌트만 추가
pPlayer->Add_Component(..., TEXT("Com_Navigation"), ...);
// → 기타 오브젝트는 Add_Component 호출 안 함
```

**절감 효과:**
- 클래스 폭발 방지: 필요한 기능의 모든 조합에 대해 클래스 생성 불필요
- 런타임 유연성: 오브젝트 생성 시점에 필요한 기능만 조합

#### **문제 3: 충돌 감지와 게임 로직의 결합**

**전통적 접근:**
```cpp
class CMonster
{
    void Update()
    {
        // 충돌 체크
        if (IsCollidingWith(player))
        {
            player->TakeDamage(10);
        }
        
        // 이동 로직
        Move();
        
        // 또 충돌 체크
        if (IsCollidingWith(otherMonster))
        {
            // ... 복잡한 처리
        }
    }
};
```

**컴포넌트 시스템:**
```cpp
// Collider 컴포넌트는 Collision Manager가 관리
// Monster는 충돌 이벤트만 수신
void CMonster::Collided_With(CCollider* pOther, CCollider::COLLISION_STATE state)
{
    if (state == ENTER && pOther->Get_ColliderTag() == TEXT("Player"))
    {
        // 충돌 시 처리
    }
}
```

**절감 효과:**
- 책임 분리: Collision Manager가 충돌 감지, GameObject가 반응
- 성능 최적화: 중앙집중식 충돌 검사로 효율성 향상
- 확장성: 새로운 충돌 타입 추가 시 Collision Manager만 수정

#### **문제 4: PartObject의 복잡한 계층 구조**

**없다면?**
```cpp
// 플레이어의 모든 부품을 하나의 클래스에 구현
class CPlayer
{
    _float3 m_BodyPosition;    // Body 위치
    _float3 m_WeaponPosition;  // Weapon 위치
    _float3 m_ArmorPosition;   // Armor 위치
    
    void Update()
    {
        // Body 업데이트
        UpdateBody();
        // Weapon 업데이트
        UpdateWeapon();
        // Armor 업데이트
        UpdateArmor();
        // ... 무한 반복
    }
};
```

**컴포넌트 + PartObject 시스템:**
```cpp
// Player는 PartObject들을 관리만 함
class CPlayer : public CContainerObject
{
    void Update(_float fTimeDelta)
    {
        // PartObject들은 자신의 컴포넌트를 자동 업데이트
        for (auto& part : m_PartObjects)
            part.second->Update(fTimeDelta);
    }
};

// 각 PartObject는 독립적인 컴포넌트 보유
class CPartObject
{
    CTransform* m_pTransform;  // 독립적인 Transform
    CModel* m_pModel;          // 독립적인 Model
    CCollider* m_pCollider;    // 독립적인 Collider
    // → 재사용 가능한 구조
};
```

**절감 효과:**
- 계층적 관리: ContainerObject → PartObject → Component
- 부품 교체 용이: 무기 변경 시 Weapon PartObject만 교체
- 독립적 충돌: Body Collider와 Weapon Collider를 별도로 관리

### 3.7 성능 및 메모리 최적화 관점

#### **메모리 효율성**

```cpp
// 예: 몬스터 중 일부만 Navigation 컴포넌트 필요
class CMonster_WalkingPath : public CMonster
{
    // Navigation 컴포넌트 추가
    Add_Component(..., TEXT("Com_Navigation"), ...);
};

class CMonster_Flying : public CMonster
{
    // Navigation 컴포넌트 없음 (날아다니므로 불필요)
};
```

**효과:**
- 필요한 메모리만 사용
- Navigation 데이터가 없는 몬스터는 해당 메모리 비용 없음
- 컴포넌트는 프로토타입 패턴으로 공유되어 메모리 중복 최소화

#### **프레임별 업데이트 최적화**

```cpp
// Collision Manager는 활성화된 Collider만 검사
void CCollision_Manager::Update()
{
    for (auto& pCollider : m_Colliders)
    {
        if (!pCollider->Get_ColliderActive())  // 비활성화 스킵
            continue;
            
        // 활성화된 Collider만 충돌 검사
    }
}
```

**효과:**
- 불필요한 충돌 검사 회피
- Collider 활성/비활성으로 상황별 최적화
- Collision Manager가 모든 Collider를 한 곳에서 효율적으로 관리

### 3.8 개발 생산성 향상

#### **재사용 가능한 컴포넌트 라이브러리**

```cpp
// 한 번 작성된 컴포넌트는 모든 오브젝트에 사용
CCollider* pCollider = CCollider::Create(pDevice, pContext, TYPE_SPHERE);

// Player에 추가
pPlayer->Add_Component(..., TEXT("Com_Collider"), &pCollider);

// Monster에 추가
pMonster->Add_Component(..., TEXT("Com_Collider"), &pCollider);

// Item에 추가
pItem->Add_Component(..., TEXT("Com_Collider"), &pCollider);

// → 컴포넌트는 완전히 재사용 가능
```

**효과:**
- 컴포넌트 개발을 한 번만 수행
- 버그 수정 시 모든 오브젝트에 자동 반영
- 신규 오브젝트 추가 시 기존 컴포넌트 조합만으로 구현 가능

#### **병렬 개발 가능**

```cpp
// 팀원 A: Collider 컴포넌트 개발
class CCollider : public CComponent { ... };

// 팀원 B: Navigation 컴포넌트 개발
class CNavigation : public CComponent { ... };

// 팀원 C: Transform 컴포넌트 사용하여 게임 로직 개발
// → 의존성 최소화로 병렬 작업 가능
```

**효과:**
- 컴포넌트별 독립적 개발
- 인터페이스만 정의되면 병렬 진행 가능
- 컴포넌트 변경이 다른 컴포넌트에 영향 최소화

## 4. 렌더링 시스템

### 4.1 CRenderer
- 렌더링 그룹별로 객체 관리
- 다중 렌더 패스를 지원
- 프레임별 렌더링 최적화

### 4.2 CPipeLine
- 전역 변환 행렬 관리
- World, View, Projection 행렬
- 역행렬 캐싱으로 성능 최적화

### 4.3 셰이더 시스템
- HLSL 기반 셰이더
- Effects11 프레임워크 사용
- 다중 셰이더 타입 지원:
  - VtxPosLine: 선 렌더링
  - VtxCube: 큐브 렌더링
  - VtxMesh: 메시 렌더링
  - VtxAnimMesh: 애니메이션 메시
  - VtxNorTex: 노말맵 메시
  - Cell: 셀 셰이딩

### 4.4 애니메이션 시스템
**CAnimation**
- Assimp를 이용한 애니메이션 로딩
- 키프레임 기반 본 애니메이션
- 채널(Channel) 단위 보간
- Slerp/lerp를 이용한 부드러운 전환

**CChannel**
- 본 단위 애니메이션 트랙
- Scale, Rotation, Translation 키프레임
- 애니메이션 블렌딩 지원

**CBone**
- 스켈레톤 구조 관리
- 계층적 트랜스폼 변환

## 5. 충돌 시스템

### 5.1 CCollision_Manager
- 레벨별 충돌체 관리
- 레이어 단위 충돌 그룹화
- 프레임별 충돌 검사

### 5.2 CCollider (추상 클래스)
**타입:**
- `CBounding_AABB`: AABB 충돌체
- `CBounding_OBB`: OBB 충돌체
- `CBounding_Sphere`: 구 충돌체

**충돌 상태:**
- ENTER: 충돌 시작
- STAY: 충돌 지속
- EXIT: 충돌 종료

## 6. 입력 시스템

### 6.1 CInput_Manager
- 키보드/마우스 통합 관리
- 키 상태 추적: UP/DOWN/PRESSING/UP once
- 마우스 위치 및 레이 감지

### 6.2 CPicking
- 마우스 레이 캐스팅
- 3D 픽킹 처리
- 월드 좌표 변환

## 7. 레벨/씬 시스템

### 7.1 CLevel (추상 클래스)
- 레벨 초기화/업데이트/렌더링/종료
- 레이어 관리
- 프로토타입 등록

**구현 레벨:**
- `CLevel_Loading`: 로딩 화면
- `CLevel_Title`: 타이틀 화면
- `CLevel_Lounge`: 로비
- `CLevel_SoggySwamp`: 던전 스테이지

### 7.2 CLayer
- 레벨 내 게임 오브젝트 계층
- 레이어별 업데이트/렌더링
- Persistent 레이어 지원 (레벨 전환 유지)

## 8. 게임 로직 레이어

### 8.1 CPlayer
- 플레이어 캐릭터 시스템
- 인벤토리 시스템
- 무기 시스템 (활, 글레이브 등)
- FSM 기반 상태 관리
- 네비게이션 기반 이동

**특수 기능:**
- 몬스터 추적 및 공격
- 화살 발사 시스템
- 회피 이동
- HP 관리

### 8.2 CMonster 계열
- 다양한 몬스터 타입
- 각자 고유한 FSM 상태
- AI 기반 행동 패턴

**구현 몬스터:**
- `CZombie`: 좀비 (공격, 휴식, 스턴 등)
- `CBabyZombie`: 작은 좀비
- `CSkeleton`: 스켈레톤 (활 공격)
- `CSlime_Large/Medium`: 슬라임

### 8.3 Weapon System
- `CWeapon_Bow`: 장궁
- `CWeapon_ShortBow`: 단궁
- `CWeapon_Glaive`: 글레이브

### 8.4 Arrow System
- `CPlayer_Arrow`: 플레이어 화살
- `CMonster_Arrow`: 몬스터 화살
- 오브젝트 풀링 패턴 적용

### 8.5 Inventory System
- `CInventoryData`: 인벤토리 데이터
- `CInventorySlot`: 슬롯 시스템
- `CInventoryGearSlot`: 장비 슬롯
- `CInventoryArtifactSlot`: 아티팩트 슬롯
- `CInventoryStoreSlot`: 상점 슬롯

## 9. 이벤트 시스템

### 9.1 CEventBus
- 이벤트 기반 통신 (Event-Driven Architecture)
- Publish-Subscribe 패턴
- 컴포넌트 간 느슨한 결합

## 10. 네비게이션 시스템

### 10.1 CNavigation
- Navigation Mesh 기반 이동
- 지형 추적
- 경로 탐색 지원

## 11. UI 시스템

### 11.1 CUI_Manager
- UI 오브젝트 생명주기 관리
- 레이어 단위 UI 관리
- Lifetime 관리 (Global, Level 등)

### 11.2 CUIObject
- UI 기본 클래스
- 렌더 순서 관리

### 11.3 특수 UI
- `CPlayerHP`: HP 표시
- `CUI_Image`: 이미지 UI

## 12. 아키텍처 패턴

### 12.1 적용된 디자인 패턴
1. **컴포넌트 패턴**: 재사용 가능한 기능 모듈화
2. **싱글톤 패턴**: GameInstance 등 전역 관리자
3. **프로토타입 패턴**: 객체 생성 및 복제
4. **전략 패턴**: FSM 상태별 행동 캡슐화
5. **옵저버 패턴**: EventBus를 통한 이벤트 구독/발행
6. **객체 풀 패턴**: ArrowPool, MonsterPool 등

### 12.2 데이터 흐름
```
프레임 시작
    ↓
Input_Device.Update()
    ↓
Input_Manager.Update_Key()
    ↓
Object_Manager.Priority_Update()
    ↓
Object_Manager.Update()
    ↓
PipeLine.Update()
    ↓
Object_Manager.Late_Update()
    ↓
Collision_Manager.Update()
    ↓
Renderer.Draw()
    ↓
프레임 종료
```

## 13. 확장성 및 유지보수

### 13.1 특징
- 컴포넌트 기반으로 기능 추가 용이
- State 패턴으로 행동 확장성 보장
- Manager 시스템으로 책임 분리
- 프로토타입 패턴으로 객체 생성 최적화

### 13.2 성능 최적화
- 오브젝트 풀링으로 메모리 관리
- 렌더 그룹 단위 배칭
- 불필요한 업데이트 제거
- Persistent 레이어로 로딩 최소화

## 14. 의존성

### 14.1 주요 라이브러리
- **DirectX 11**: 3D 그래픽 렌더링
- **DirectXTK**: 유틸리티 및 헬퍼
- **DirectInput**: 입력 처리
- **Assimp**: 3D 모델/애니메이션 로딩
- **Effects11**: 셰이더 관리

## 15. 결론

본 프로젝트는 컴포넌트 기반 아키텍처와 FSM 패턴을 통해 확장 가능하고 유지보수가 용이한 게임 엔진을 구현하였습니다. 
모든 게임 객체는 재사용 가능한 컴포넌트로 구성되며, 상태 기반 AI 시스템으로 캐릭터 행동을 유연하게 제어합니다.
DirectX 11과 최신 게임 엔진 설계 패턴을 활용하여 3D 액션 게임에 필요한 모든 시스템을 구현한 종합 게임 엔진입니다.

