# 정점 피킹(Picking Vertex) 시스템

## 1. 개요

네비게이션 메시를 직접 생성하기 위해 구현한 정점 피킹 기능입니다. 마우스 레이를 사용하여 Model -> Mesh -> Vertex 단계로 내려가면서 가장 가까운 정점을 선택합니다.

### 설계 목적
- **네비게이션 메시 수동 생성**: 게임 월드에서 직접 정점을 선택하여 네비게이션 셀 구성
- **정확한 위치 지정**: 마우스 커서 위치에서 가장 가까운 메시의 정점을 찾아냄
- **로컬/월드 좌표 변환**: 행렬 변환을 통해 정확한 좌표 계산

---

## 2. 전체 호출 흐름

```
사용자 입력 (마우스 클릭)
    ↓
CPicking::Picked_Vertex()
    ↓
CModel::Picking_Vertex()
    ↓
CMesh::Picking_Vertex()
    ↓
CMesh::Ray_Intersects_Sphere()
```

---

## 3. 상세 구현

### 3.1 CPicking::Picked_Vertex() - 최상위 진입점

**위치**: `Engine/Private/Picking.cpp` (49-68줄)

**역할**: 
- 마우스 레이를 월드 공간으로 계산
- GameObject에서 Model과 Transform 컴포넌트를 찾아서 전달

**핵심 코드**:
```cpp
_bool CPicking::Picked_Vertex(_float3& fLocalPickedVertex, const _wstring& strGameObjectTag, 
                              _uint iLayerLevelIndex, const _wstring& strLayerTag)
{
    _float4  fWorldMousePos = {};
    _float3  fWorldMouseRay = {};

    // 1. 마우스 스크린 좌표 → 월드 공간 레이로 변환
    Compute_MouseRay(fWorldMousePos, fWorldMouseRay);

    // 2. GameObject에서 Model과 Transform 컴포넌트 찾기
    CGameObject* pPickedObject = m_pGameInstance->Find_GameObject(...);
    CModel* pPickedObjModelCom = dynamic_cast<CModel*>(...);
    CTransform* pPickedObjTransformCom = dynamic_cast<CTransform*>(...);
    
    // 3. World Matrix 가져오기
    const _float4x4& pPickedObjWorldMatrix = pPickedObjTransformCom->Get_WorldMatrix();

    // 4. Model의 Picking_Vertex 호출 (로컬 좌표로 결과 반환)
    if (pPickedObjModelCom->Picking_Vertex(fWorldMousePos, fWorldMouseRay, 
                                           fLocalPickedVertex, pPickedObjWorldMatrix))
    {
        return true;
    }

    return false;
}
```

**주요 변환 과정**:
1. **Compute_MouseRay()**: 스크린 좌표 → NDC → 뷰 공간 → 월드 공간 레이
   - InvProjMatrix와 InvViewMatrix를 사용하여 역변환
   - 카메라 위치와 레이 방향 벡터 계산

---

### 3.2 CModel::Picking_Vertex() - Model 단계 처리

**위치**: `Engine/Private/Model.cpp` (187-227줄)

**역할**:
- 월드 공간 레이를 로컬 공간으로 변환
- 모든 Mesh를 순회하며 가장 가까운 정점 찾기
- BoundingBox로 먼저 걸러내어 성능 최적화

**핵심 코드**:
```cpp
_bool CModel::Picking_Vertex(const _float4& worldMousePos, const _float3& worldMouseRay, 
                             _float3& vOutPickedVertex, const _float4x4& WorldMatrix) const
{
    _float  fMinDist = FLT_MAX;
    _bool   bHit = false;

    // ★ 중요: 월드 공간 → 로컬 공간 변환
    _matrix matInvWorld = XMMatrixInverse(nullptr, XMLoadFloat4x4(&WorldMatrix));
    _vector vLocalOrigin = XMVector3TransformCoord(XMLoadFloat4(&worldMousePos), matInvWorld);
    _vector vLocalDir = XMVector3TransformNormal(XMLoadFloat3(&worldMouseRay), matInvWorld);
    vLocalDir = XMVector3Normalize(vLocalDir);

    _float3 localMousePos, localMouseRay;
    XMStoreFloat3(&localMousePos, vLocalOrigin);
    XMStoreFloat3(&localMouseRay, vLocalDir);

    // 모든 Mesh 순회
    for (auto& pMesh : m_Meshes)
    {
        // BoundingBox AABB 충돌 체크 (성능 최적화)
        if (false == pMesh->Check_BoundingBox_AABB(localMousePos, localMouseRay))
            continue;

        _float3 vLocalPickedVertice = {};
        _bool   bMeshHit = false;
        _float  fOutDist = {};

        // Mesh에서 정점 피킹 시도
        bMeshHit = pMesh->Picking_Vertex(localMousePos, localMouseRay, 
                                         vLocalPickedVertice, fOutDist, 0.3f);

        if (bMeshHit)
        {
            // 가장 가까운 정점만 저장
            if (fOutDist < fMinDist)
            {
                fMinDist = fOutDist;
                vOutPickedVertex = vLocalPickedVertice;  // 로컬 좌표로 반환
                bHit = true;
            }
        }
    }
    
    return bHit;
}
```

**행렬 변환 요약**:
1. **InvWorld Matrix 계산**: `XMMatrixInverse()`로 월드 행렬의 역행렬 계산
2. **원점 변환**: `XMVector3TransformCoord()` - 위치 변환 (w=1 처리)
3. **방향 벡터 변환**: `XMVector3TransformNormal()` - 방향 변환 (w=0 처리, 스케일 영향 없음)

**왜 로컬 공간으로 변환하는가?**
- Mesh의 정점들은 로컬 공간 좌표로 저장되어 있음
- 월드 공간의 레이를 로컬 공간으로 변환하면 직접적인 거리 계산 가능
- 여러 Transform을 가진 오브젝트도 각각의 로컬 공간에서 처리 가능

---

### 3.3 CMesh::Picking_Vertex() - Mesh 단계 처리

**위치**: `Engine/Private/Mesh.cpp` (392-419줄)

**역할**:
- Mesh의 모든 정점을 순회
- 각 정점 주변에 구(Sphere)를 설정하고 레이와의 교차 판정
- 가장 가까운 정점 선택

**핵심 코드**:
```cpp
_bool CMesh::Picking_Vertex(const _float3& localMousePos, const _float3& localMouseRay,
                            _float3& vOutPickedVertex, _float& fOutDist, 
                            _float fThresholdRadius) const
{
    _vector vDir = XMVector3Normalize(XMLoadFloat3(&localMouseRay));
    _float3 vRay = {};
    XMStoreFloat3(&vRay, vDir);

    _bool  bHit = false;
    _float fMinDist = FLT_MAX;

    // 모든 정점 순회
    for (_uint i = 0; i < m_iNumVertices; ++i)
    {
        _float fDist = {};

        // 정점 주변에 구를 설정하고 레이와의 교차 판정
        if (Ray_Intersects_Sphere(localMousePos, vRay, m_pVertices[i], 
                                  fThresholdRadius, fDist))
        {
            if (fDist < fMinDist)
            {
                fMinDist = fDist;
                XMStoreFloat3(&vOutPickedVertex, XMLoadFloat3(&m_pVertices[i]));
                fOutDist = fDist;
                bHit = true;
            }
        }
    }

    return bHit;
}
```

**파라미터 설명**:
- `fThresholdRadius` (기본값 0.3f): 정점 주변에 설정할 구의 반지름
  - 이 값보다 가까이 있는 정점만 선택 가능
  - 값을 크게 하면 선택 범위가 넓어지고, 작게 하면 정확도가 높아짐

---

### 3.4 CMesh::Ray_Intersects_Sphere() - 구-레이 교차 판정

**위치**: `Engine/Private/Mesh.cpp` (421-449줄)

**역할**:
- 레이와 구(Sphere)의 교차 판정 알고리즘
- 정점 주변 반경 내에 레이가 지나가는지 확인
- 교차 지점까지의 거리 계산

**핵심 코드**:
```cpp
_bool CMesh::Ray_Intersects_Sphere(const _float3& localMousePos, const _float3& localMouseRay, 
                                   const _float3& sphereCenter, _float sphereRadius, 
                                   _float& outDistance) const
{
    _vector localMouseOrigin = XMLoadFloat3(&localMousePos);
    _vector localRayDir = XMVector3Normalize(XMLoadFloat3(&localMouseRay));
    _vector center = XMLoadFloat3(&sphereCenter);

    _vector toCenter = center - localMouseOrigin;

    // 레이 방향으로 정점까지의 투영 길이 계산
    _float projectionLength = XMVectorGetX(XMVector3Dot(toCenter, localRayDir));

    // sphere가 ray 반대 방향에 있는 경우
    if (projectionLength < 0.f)
        return false;

    // 레이에서 구 중심까지 가장 가까운 점
    _vector closestPoint = localMouseOrigin + localRayDir * projectionLength;
    _vector diff = center - closestPoint;  // 가장 가까운 점에서 구 중심까지의 거리

    _float sqDistToCenter = XMVectorGetX(XMVector3LengthSq(diff));
    _float radiusSquared = sphereRadius * sphereRadius;

    // 거리의 제곱이 반지름의 제곱보다 크면 교차하지 않음
    if (sqDistToCenter > radiusSquared)
        return false;

    // 교차 확인, 거리 반환
    outDistance = projectionLength;
    return true;
}
```

**알고리즘 설명**:
1. **투영 길이 계산**: 레이 원점에서 정점(구 중심)까지의 벡터를 레이 방향으로 투영
2. **최근접점 계산**: 레이 상에서 구 중심에 가장 가까운 점 계산
3. **거리 검사**: 최근접점에서 구 중심까지의 거리가 반지름보다 작으면 교차
4. **거리 반환**: 교차하는 경우, 레이 원점에서 최근접점까지의 거리 반환

---

## 4. 좌표계 변환 체인

### 4.1 변환 흐름도

```
스크린 좌표 (마우스 클릭 위치)
    ↓ [InvProjMatrix]
뷰 공간 좌표
    ↓ [InvViewMatrix]
월드 공간 좌표 (fWorldMousePos, fWorldMouseRay)
    ↓ [InvWorldMatrix] (CModel::Picking_Vertex에서)
로컬 공간 좌표 (localMousePos, localMouseRay)
    ↓ [정점 비교]
선택된 정점 (로컬 좌표로 반환)
    ↓ [WorldMatrix] (사용하는 쪽에서 필요시 변환)
월드 좌표 (예: LoungeMap.cpp에서 변환)
```

### 4.2 실제 사용 예시

**위치**: `Client/Private/LoungeMap.cpp` (67-80줄)

```cpp
_float3 fLocalPickedVertex = {};

// 로컬 좌표로 정점 반환받음
if (m_pGameInstance->Picked_Vertex(fLocalPickedVertex, TEXT("GameObject_LoungeMap"), 
                                    LEVEL_LOUNGE, TEXT("Layer_BackGround")))
{
    // 필요시 월드 좌표로 변환
    _vector vWorldPickedVertex = XMVector3TransformCoord(
        XMLoadFloat3(&fLocalPickedVertex), 
        XMLoadFloat4x4(m_pTransformCom->Get_WorldMatrix_Ptr())
    );
    
    _float3 fWorldPickedVertex;
    XMStoreFloat3(&fWorldPickedVertex, vWorldPickedVertex);
    
    // 네비게이션 메시 생성에 사용
    // ...
}
```

---

## 5. 성능 최적화 기법

### 5.1 BoundingBox AABB 충돌 체크
- Model 단계에서 모든 Mesh를 검사하기 전에 BoundingBox로 1차 필터링
- 레이가 BoundingBox와 교차하지 않으면 해당 Mesh는 건너뜀

### 5.2 최소 거리 비교
- 여러 Mesh에서 정점을 찾은 경우, 가장 가까운 정점만 저장
- `fMinDist`와 `fOutDist`를 비교하여 최적 선택

### 5.3 정점 순회 최적화
- Mesh 내부에서 모든 정점을 순회하지만, Threshold Radius로 범위 제한
- 교차 판정 실패 시 즉시 다음 정점으로 이동

---

## 6. 주요 특징

### 6.1 로컬 좌표 반환
- 피킹된 정점은 **로컬 좌표**로 반환됨
- 이유: Mesh의 정점 데이터가 로컬 공간 기준이기 때문
- 필요시 사용하는 쪽에서 WorldMatrix를 곱하여 월드 좌표로 변환

### 6.2 Threshold Radius 설정
- 기본값: 0.3f
- 의미: 정점 주변 0.3 유닛 반경 내에 레이가 지나가야 선택됨
- 조절: 정확도와 선택 용이성의 트레이드오프

### 6.3 Pickable 플래그
- Model 생성 시 `bPickable` 플래그로 피킹 가능 여부 설정
- 예: 무기 모델은 `bPickable = false`로 설정하여 피킹에서 제외

---

## 7. 사용 시나리오

### 7.1 네비게이션 메시 생성
1. 맵 오브젝트에 `bPickable = true` 설정
2. 마우스로 지형의 정점들을 클릭
3. 선택된 정점들로 네비게이션 셀 생성
4. 로컬 좌표를 월드 좌표로 변환하여 저장

### 7.2 디버깅 도구
- 게임 중 실시간으로 메시의 정점 위치 확인
- 지형의 특정 지점을 정확히 선택하여 디버깅 정보 출력

---

## 8. 핵심 정리

1. **호출 경로**: `CPicking` → `CModel` → `CMesh` → 정점 선택
2. **좌표 변환**: 월드 공간 레이 → 로컬 공간 레이 (InvWorldMatrix 사용)
3. **선택 알고리즘**: 구-레이 교차 판정으로 가장 가까운 정점 찾기
4. **반환 값**: 로컬 좌표로 반환 (필요시 월드 좌표로 변환)
5. **최적화**: BoundingBox AABB 충돌 체크로 불필요한 Mesh 스킵

**핵심 철학:**
- "월드 공간에서 입력받고, 로컬 공간에서 처리하고, 필요시 월드 좌표로 변환"
- "Model은 Mesh를 관리하고, Mesh는 정점을 관리"
- "최소 거리 비교로 가장 정확한 정점 선택"

